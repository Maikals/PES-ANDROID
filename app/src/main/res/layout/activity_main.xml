<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    >

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/iniciar_venta"
        android:id="@+id/iniciar_venta"
        android:layout_marginTop="85dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:height="150dp"
        android:textSize="30sp"
        android:width="200dp"
        android:layout_marginBottom="20dp"
        android:onClick="iniciarVenda"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sortir"
        android:id="@+id/sortir"
        android:layout_below="@+id/iniciar_venta"
        android:layout_alignLeft="@+id/iniciar_venta"
        android:layout_alignRight="@+id/iniciar_venta"
        android:textSize="30sp"
        android:height="60dp"
        android:onClick="tancar"/>

</RelativeLayout>
